import { z } from "zod";
const result=z.object({person:z.object({first_name:z.string().nullable(),last_name:z.string().nullable(),title:z.string().nullable(),linkedin_url:z.string().nullable(),email:z.string().nullable(),email_status:z.string().nullable()}).nullable()});
export async function matchPerson(name:string,domain:string){if(!process.env.APOLLO_API_KEY)return null;const [first_name,...rest]=name.trim().split(/\s+/);const response=await fetch("https://api.apollo.io/api/v1/people/match",{method:"POST",headers:{"content-type":"application/json","x-api-key":process.env.APOLLO_API_KEY},body:JSON.stringify({first_name,last_name:rest.join(" "),domain,reveal_personal_emails:false})});if(!response.ok)throw new Error(`Apollo match failed: ${response.status}`);const person=result.parse(await response.json()).person;if(!person)return null;return {...person,email:person.email_status==="verified"?person.email:null}}
