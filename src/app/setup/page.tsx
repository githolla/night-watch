const required=["SUPABASE_URL","SUPABASE_PUBLISHABLE_KEY","SUPABASE_SECRET_KEY","ANTHROPIC_API_KEY","CRON_SECRET"];
export default function Setup(){return <main className="login"><section className="login-card" style={{width:"min(680px,92vw)"}}><div className="eyebrow">Night Watch deployment</div><h1>Configuration required</h1><p>The application deployed successfully, but its live Supabase connection is missing. Add the environment variables below in Vercel, then redeploy.</p><ol>{required.map(name=><li key={name}><code>{name}</code></li>)}</ol><p><strong>Optional later:</strong> Apollo for email enrichment, Google OAuth variables for Gmail sending, and <code>ALLOWED_EMAILS</code> if you want to replace the default Josh/Jenna access list.</p><p><strong>Vercel:</strong> Project Settings → Environment Variables → add values for Production, Preview, and Development.</p><p>Then open Deployments, select the latest deployment, and choose Redeploy.</p></section></main>}
