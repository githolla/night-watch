import { cronAuthorized } from "@/lib/auth";import { classifyReply } from "@/lib/agents";import { thread } from "@/lib/gmail";import { admin } from "@/lib/supabase/admin";
const decode=(data?:string)=>data?Buffer.from(data,"base64url").toString("utf8"):"";
export async function GET(request:Request){if(!cronAuthorized(request))return Response.json({error:"Unauthorized"},{status:401});const db=admin(),{data:touches}=await db.from("touches").select("id,card_id,sent_by,gmail_thread_id,sent_at").eq("channel","email").not("gmail_thread_id","is",null).is("reply_at",null);let replies=0;for(const touch of touches??[]){try{const value=await thread(touch.sent_by,touch.gmail_thread_id),messages=value.messages.filter(m=>Number(m.internalDate)>new Date(touch.sent_at).getTime());if(!messages.length)continue;const latest=messages.at(-1)!,body=decode(latest.payload.body?.data)||decode(latest.payload.parts?.find(p=>p.mimeType==="text/plain")?.body.data),classification=await classifyReply(body);await db.from("touches").update({reply_at:new Date(Number(latest.internalDate)).toISOString(),reply_classification:classification}).eq("id",touch.id);await db.from("cards").update({status:classification==="positive"?"positive":"replied"}).eq("id",touch.card_id);replies++}catch{}}return Response.json({replies})}
